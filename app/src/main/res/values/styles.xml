<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="CustomActionBarStyle" parent="@android:style/Theme.Holo.Light">
        <item name="android:layout_marginRight">0dp</item><!--右边距为0-->
        <item name="android:layout_marginLeft">0dp</item><!--左边距为0-->
        <item name="android:layout_marginTop">0dp</item><!--右边距为0-->
        <item name="android:layout_marginBottom">0dp</item><!--左边距为0-->

        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

</resources>
